import React from 'react';

const SectionTitle = ({ title }) => {
    return (
        <div>
            <h1 className='text-4xl text-center font-bold text-[#60A5FA]'>{title}</h1>
        </div>
    );
};

export default SectionTitle;